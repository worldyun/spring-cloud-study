server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 60.205.214.254
      port: 8500
      discovery:
        service-name: ${spring.application.name}